[package]
name = "lockness-executor"
version.workspace = true
authors.workspace = true
edition.workspace = true
description = "TODO"
repository.workspace = true
keywords = ["executor", "blocking", "thread", "parallel", "performance"]
categories = ["concurrency", "asynchronous", "data-structures"]
license.workspace = true

[dependencies]
bon = "3.9.0"

[dev-dependencies]
criterion = "0.8.2"
flume = "0.12.0"
rayon = "1.11.0"
supercilex-tests = { version = "0.4.21", default-features = false, features = ["api"] }
tokio = "1.49.0"
